{
  "name": "orchestra-mesh",
  "private": true,
  "version": "0.1.0",
  "description": "Durable distributed workflow engine",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "echo build placeholders",
    "test": "echo test placeholders",
    "lint": "echo lint placeholders"
  }
}