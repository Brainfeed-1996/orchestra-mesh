# Mutation API

## POST /api/v1/workflows
Create a new workflow with an initial created event.

## POST /api/v1/workflows/{id}/events
Append an event to an existing workflow and return the projected state.
